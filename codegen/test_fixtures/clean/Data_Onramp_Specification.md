# Data Onramp Specification

## Intake Channels

The platform supports four intake channels: `file_import`, `api_pull`, `manual_capture`, `public_data`.

## Column Mapping

Each source column is mapped to a canonical field. Resolution options: `accept`, `flag`, `reject`, `defer`.

## Review Queue

Flagged records enter a review queue for manual resolution.
